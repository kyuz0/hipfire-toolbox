diff --git a/cli/index.ts b/cli/index.ts
index 440cf474..2566e698 100644
--- a/cli/index.ts
+++ b/cli/index.ts
@@ -1109,6 +1109,7 @@ class Engine {
     const bins = [
       ...(envBin ? [envBin] : []),
       resolve(__dirname, `../target/release/examples/daemon${exe}`),
+      resolve(__dirname, `../bin/daemon${exe}`),
       join(HIPFIRE_DIR, "bin", `daemon${exe}`),
     ];
     const bin = bins.find(p => existsSync(p));
diff --git a/crates/rdna-compute/src/gemm.rs b/crates/rdna-compute/src/gemm.rs
index 1ad5bd95..5e9208db 100644
--- a/crates/rdna-compute/src/gemm.rs
+++ b/crates/rdna-compute/src/gemm.rs
@@ -13723,10 +13723,67 @@ impl Gpu {
         batch_size: usize,
     ) -> HipResult<()> {
         self.bind_thread()?;
+        if self.arch_caps.is_rdna4() {
+            return self.gemm_q8_0_wmma_gfx12(a, x, y, m, k, batch_size);
+        }
         debug_assert_eq!(k % 32, 0, "gemm_q8_0_wmma: K must be a multiple of 32 (got K={k})");
-        debug_assert!(self.arch_caps.is_rdna4(),
-            "gemm_q8_0_wmma: gfx12 only (got arch {})", self.arch);
+        self.ensure_kernel(
+            "gemm_q8_0_wmma",
+            kernels::GEMM_Q8_0_WMMA_SRC,
+            "gemm_q8_0_wmma",
+        )?;
+        let x_f16_ptr = self.ensure_fp16_x(x, batch_size * k)?;
+
+        let mut a_p = a.buf.as_ptr();
+        let mut xp = x_f16_ptr;
+        let mut y_p = y.buf.as_ptr();
+        let mut m_val = m as i32;
+        let mut k_val = k as i32;
+        let mut n_val = batch_size as i32;
+
+        let mut params: Vec<*mut c_void> = vec![
+            &mut a_p as *mut _ as *mut c_void,
+            &mut xp as *mut _ as *mut c_void,
+            &mut y_p as *mut _ as *mut c_void,
+            &mut m_val as *mut _ as *mut c_void,
+            &mut k_val as *mut _ as *mut c_void,
+            &mut n_val as *mut _ as *mut c_void,
+        ];
+
+        let row_tiles = (m + 15) / 16;
+        let batch_tiles = (batch_size + 15) / 16;
+        let bytes = m * (k / 32) * 34
+                  + batch_size * k * 2
+                  + batch_size * m * 4;
+        let timer = crate::profile::begin_timer(&self.hip, "gemm", "gemm_q8_0_wmma", bytes);
+        let result = self.launch_maybe_blob(
+            "gemm_q8_0_wmma",
+            [row_tiles as u32, batch_tiles as u32, 1],
+            [32, 1, 1],
+            0,
+            &mut params,
+            || {
+                let mut b = hip_bridge::KernargBlob::new();
+                b.push_ptr(a_p); b.push_ptr(xp); b.push_ptr(y_p);
+                b.push_i32(m_val); b.push_i32(k_val); b.push_i32(n_val);
+                b
+            },
+        );
+        if let Some(t) = timer { t.finish(&self.hip); }
+        result
+    }
+
+    pub fn gemm_q8_0_wmma_gfx12(
+        &mut self,
+        a: &GpuTensor,
+        x: &GpuTensor,
+        y: &GpuTensor,
+        m: usize,
+        k: usize,
+        batch_size: usize,
+    ) -> HipResult<()> {
         self.bind_thread()?;
+        debug_assert_eq!(k % 32, 0, "gemm_q8_0_wmma_gfx12: K must be a multiple of 32 (got K={k})");
         self.ensure_kernel(
             "gemm_q8_0_wmma_gfx12",
             kernels::GEMM_Q8_0_WMMA_GFX12_SRC,
